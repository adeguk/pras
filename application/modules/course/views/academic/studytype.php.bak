<div class="admin-box">
    <h3>List of Current Study Type</h3>

    <?php echo form_open(SITE_AREA.'/academic/course/delete_studytype'); ?>
    <table class="table table-striped">
        <thead>
			<tr>
				<th class="column-check"><input class="check-all" type="checkbox" /></th>
				<th>Study Type</th>
				<th>Description</th>
				<th style="width: 20%">Status</th>
			</tr>
		</thead>
		<tfoot>
			<tr>
				<td colspan="4">With selected:&nbsp;
					<input type="submit" name="submit" class="btn btn-danger" id="delete-me" value="<?php echo lang('bf_action_delete') ?>" onclick="return confirm('Are you sure you want to delete this Study Type(s)?')">
				</td>
			</tr>
		</tfoot>
		<tbody>
		<?php if (isset($studytypes) && is_array($studytypes)) :
			foreach ($studytypes as $studytype) : ?>
			<tr>
				<td><input type="checkbox" name="checked[]" value="<?php echo $studytype->studyTypeID ?>" /></td>
				<td>
					<a href="<?php echo site_url(SITE_AREA .'/academic/course/edit_studytype/'. $studytype->studyTypeID) ?>">
						<?php e($studytype->studyTypeName); ?>
					</a>
				</td>
				<td><?php e($studytype->description); ?></td>
				<td><?php e($studytype->status=='1'?'Enabled':'Disabled'); ?></td>
			</tr>
			<?php endforeach;
            else: ?>
			<tr>
				<td colspan="4">
					<br/>
					<div class="alert alert-warning">
						No Study Type found!
					</div>
				</td>
			</tr>
		<?php endif; ?>
		</tbody>
    </table>
</div>

